# Changes
# - Added field is_fatal to message

info:
  version: 1.0.2
components:
  schemas:
    Error:
      required:
        - code
        - message
        - is_fatal
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string
        is_fatal:
          type: bool