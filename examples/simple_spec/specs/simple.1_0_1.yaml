info:
  version: 1.0.1
components:
  schemas:
    Caretaker:
      required:
        - id
        - name
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        tag:
          type: string